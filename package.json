{
	"name": "rosalind",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "npm run build && node ./js",
		"test": "npm run build && jasmine",
		"build": "rm -rf ./js && tsc -t \"ESNEXT\" --pretty"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/jasmine": "^3.3.12",
		"@types/node": "^12.0.2",
		"jasmine": "^3.4.0"
	}
}
